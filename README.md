### Aplikacje w React z zastosowaniem następujących narzędzi:
- GraphQL + Apollo
- NodeJS
- MongoDB

Folder `users` jest wstępem do zapoznania się z GraphQL. `Lyrical-GraphQL` jest aplikacją do dodawania piosenek razem z tekstem. 